CREATE TABLE test (
  id INT, first_name VARCHAR(200)
  );
  
  INSERT INTO test (id, first_name)
  VALUES
  (1, 'Jyuan'),
  (2, ''),
  (3, NULL),
  (4, 'NICE'),
  (5, ''),
  (6, 'Carlos');
 
 
 
 ОТДЕЛЬНО ЗАПРОС

  SELECT * FROM test;
